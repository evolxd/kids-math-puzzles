# HANDOFF.md — 思维闯关 / kids-math-puzzles

2026-08-03 · 目的：让一个全新会话（不带任何记忆/上下文）只靠这份文档就能无缝接手。按顺序读完本文档即可，不需要额外提问就能开始干活；文末列了仍需要人决定的开放问题。

---

## 0. 项目是什么

- **仓库**：`C:\Users\evolx\kids-math-puzzles\`（本地路径即工作目录），git，remote `https://github.com/evolxd/kids-math-puzzles.git`，分支 `master`，**仓库是公开的**。
- **线上**：`https://evolxd.github.io/kids-math-puzzles/`，push到master后GitHub Pages自动重建（通常几分钟内生效）。
- **定位**：免费、无账号、纯静态站，面向"其他家庭"（不只是自己孩子）的思维/逻辑题训练产品，1-7年级规划，当前优先做4-7年级+一个跨年级的PK对战功能。**不是商用**，但网站本身公开可访问。
- **和另一个项目的关系**：`C:\Users\evolx\奥数辅导` 是完全独立的另一个项目（给用户自己孩子做的奥数辅导teach工作区），**不要混淆、不要往一起并**。

---

## 1. 文件结构总览

```
kids-math-puzzles/
├── index.html          最初的100题合集，历史遗留，刻意保持不动，没并入年级体系
├── grades.html          年级选择落地页 + PK对战独立入口卡片
├── grade3-4.html         三四年级，6题试点，已用"探险地图"(advmap-*)风格header
├── grade5-6.html         五六年级，60题，最完整，含World 7"AI时代思维"，仍是旧版hud/hero header
├── grade7-plus.html      七年级+挑战，43题，footer里有PK对战入口按钮
├── shared.css            所有年级页共用样式（题卡/图示/家长中心/深浅色/探险地图）
├── shared.js             所有年级页共用逻辑（进度存取/家长中心/统计图表/探险地图渲染）
├── pk.html               PK对战擂台页面（新功能，本文档重点）
├── pk.js                 PK对战：Supabase客户端 + 题库数据 + 判分逻辑
├── pk-ui.js              PK对战：页面状态机/DOM渲染（创建/加入/答题/等待/结果）
├── tools/split_tiers.js  从index.html原题库按TIER3/TIER4分类重新生成grade5-6/7-plus.html的构建脚本
├── docs/
│   ├── architecture.md              认知科学向全局产品架构（技能标签/掌握度/今日挑战），Phase 1未开始实现
│   ├── pk-mode-design.md            PK对战模式产品设计文档（已批准，已实现）
│   └── pk-question-bank-draft.md    PK题库13题的起草+审核记录（DeepSeek起草，Claude逐题核算）
├── supabase/
│   └── pk-schema.sql     PK对战的数据库schema + RLS策略 + 已知安全取舍说明
└── HANDOFF.md            本文档
```

---

## 2. 已完成的工作（按主题，非严格时间顺序）

### 2.1 多年级拆分（早期工作，已稳定，未在本次会话改动）
- 原100题按真实难度（参考Math Kangaroo/MOEMS的分级校准，不是照抄题目）重新归类：约57题够格五六年级、约43题是七年级以上/世界级难题（Cheryl生日题等），**没删，标成挑战题**。分类逻辑在`tools/split_tiers.js`（TIER3/TIER4数组），改数组后`node tools/split_tiers.js`重新生成对应页面。
- `shared.css`/`shared.js`是跨年级页面的公共逻辑；`index.html`自己保留独立内嵌CSS/JS副本（历史遗留，故意不碰）。
- localStorage key按年级加前缀：`thinking.<tier>.solved.v1` / `.dates.v1` / `.grades.v1` / `.answers.v1`，tier=g34/g56/g7p。`thinking.theme`（深浅色偏好）不加前缀，全站共用。
- 家长中心：历史统计**永久保留**——"重置打卡进度"和取消打勾只清`solved`，不碰`dates`/`grades`。按天/周/月/年切换，SVG柱状图（完成题数+正确率）。
- World 7"AI时代思维"（仅grade5-6.html）：3道具体题型——AI解题纠错（代入验算）、逻辑谬误识别（必要非充分陷阱）、信息充分性判断，不是空泛贴标签。

### 2.2 探险地图UI试点（grade3-4.html）
- 暖色路径+节点的游戏化header，替代旧的hud/hero结构，`shared.css`里的`.advmap-*`class族 + `shared.js`里的`buildAdvMap()`函数。
- 深浅色主题：CSS token化（`--adv-*`变量），**踩过的坑**——`:root:not([data-theme="light"])`选择器必须包在`@media (prefers-color-scheme: dark)`里，否则会错误匹配所有默认浅色OS的用户，这个bug犯过两次才彻底改成token模式。
- 修了两个真bug：①`shared.js`的世界内题目查询原来靠`data-q^="世界id-"`前缀匹配，grade3-4.html的题用的是`g34-`前缀（跟世界id`m1`/`m2`不一致），导致进度永远卡0——改用DOM包含关系查询`questsInWorld(w)`修复。②家长中心批改后没触发`render()`，导致星星数不刷新——批改按钮处理函数末尾补了`render()`调用。

### 2.3 docs/architecture.md（认知科学架构文档，已写完，**Phase 1还没开始实现**）
- 核心概念：现有"世界"分类之下再加一层"技能标签"（一题可以有1-3个技能），技能才是自适应推荐的最小单位。
- 掌握度模型：Lv.0未接触/Lv.1初步/Lv.2半熟（最近2题独立答对）/Lv.3精通（最近3题独立答对**且分布在≥2个不同自然日**——这条把"间隔"直接写进定义，防止一口气刷完被误判精通）。
- 推荐规则：在Lv.1/Lv.2技能里挑掌握度最低的，出一道没做过的新题；都到Lv.3才开新技能。
- "今日挑战"：交织练习入口，5-6题，强制跨世界，配1道热身+1道尝鲜。
- 为什么不用经典间隔重复算法：每题是独特推理题不是事实卡片，重复原题没有巩固价值，间隔重复的原则要保留但载体必须是同技能新题。
- 分阶段：Phase 1（给现有166题打技能标签，纯数据工作零界面风险）→ Phase 2（掌握度计算+家长中心技能地图）→ Phase 3（今日挑战入口）→ Phase 4（探险地图路径按技能分组、星星换成掌握度可视化）。**目前卡在Phase 1还没开始**。

### 2.4 PK对战模式（本次会话主要工作，已完整上线）

**起因**：用户孩子反馈七年级+的题"都是work"，用一整轮`/grill-me`会话诊断出根子是"独自解孤立谜题没意义感"（不是页面不好看），孩子提到喜欢Prodigy式RPG对战，明确要真实PK（不只是单人闯关）。

**产品策略**（docs/pk-mode-design.md）：不跟Prodigy拼规模（拼不过），拼它做不到的地方——胜负只认答对/答得快不叠装备RNG（更公平也更省事）、PK题保持"智力题"含金量不退化成纯算术（否则PK和精心设计的深度题库比就没意义）、异步优先不做真实时同步（工程量小一个数量级）、继续免费无账号。

**范围决策**（都经过跟用户逐条确认）：
- 对手：**房间邀请码制，不做陌生人自动匹配**——涉及别人家孩子实时互动的儿童隐私合规（COPPA/PIPEDA），一个人维护的免费项目扛不住，明确列入"以后再评估"清单，不是本版做的
- 同步：**异步**——各自答完存结果，系统比对，不用同时在线，不做真实时presence/断线重连那套复杂度
- 计分：**答对轮数+用时**直接决定胜负，不叠装备/等级/随机数

**技术实现**：
- `pk.html`：单页应用，链接`shared.css`拿基础token，自己一套`.pk-*`样式（不复用`.quest`等现有class，因为PK需要计时和自动判分，跟"家长事后批改"模型不同）。CDN引入`@supabase/supabase-js@2`。
- `pk.js`：
  - `SUPABASE_URL`/`SUPABASE_KEY`（publishable key，公开无妨，安全靠RLS不靠藏key）硬编码在文件顶部
  - `QUESTIONS`数组：17道题（详见2.5），每题字段：`id`/`en`/`zh`/`hintEn`/`hintZh`/`answerEn`/`answerZh` + 判分字段(`accept`/`acceptAll`/`matchLetter`/`matchAny`/`type:'yesno'+correct`二选一) + `category`(`'puzzle'`|`'academic'`) + `tier`(`'g34'`|`'g7p'`)
  - `checkAnswer(question, userInput)`：判分核心函数，按question的判分字段类型分支（见2.6"判分方式"）
  - `pickQuestionSet(n, tier)`：只从`category==='puzzle' && tier===参数`的题里洗牌抽n题——**当前只从智力题抽，学术题(academic)完全不进战局，是刻意决定**（见2.7）
  - `generateRoomCode()`：6位房间码，字符集排除0/O/1/I/L避免视觉混淆
  - `createRoom(questionCount, tier)`/`getRoom(code)`/`joinRoom(code, nickname)`/`submitAnswers(participantId, answers)`/`getParticipants(code)`：Supabase CRUD封装，都是`window.PK`导出的公开API
- `pk-ui.js`：纯状态机+DOM渲染，`state`对象（mode/code/questionIds/participantId/nickname/qIndex/myAnswers/qStartedAt/pollTimer）闭包在IIFE里不暴露到window。流程：`renderLanding()`（创建/加入两个tab）→`renderCreateForm()`（含年级/难度下拉选择器）或`renderJoinForm()`→创建者到`renderShareCode()`（可以立刻开始不用等朋友加入）→`startPlaying()`→`renderQuestion()`（逐题作答，`type==='yesno'`的题渲染按钮不是输入框）→答完全部题目`finishAndSubmit()`→`renderWaiting()`（每3秒轮询`pollForResults()`）→双方都交卷后`renderResults()`（排序：答对数降序，同分按用时升序）

**Supabase**：
- 项目名`kids-math-puzzles-pk`，org "Maths"，region Americas，建项目时**故意关掉**"Automatically expose new tables"、**故意开启**"Enable automatic RLS"（安全默认值更紧）
- 两张表：`pk_rooms`（id=房间码/question_set jsonb/created_at/expires_at默认+48h/status）、`pk_participants`（id uuid/room_id/nickname/answers jsonb/joined_at）
- **没有账号体系**，房间码本身就是访问控制（随机生成、猜不到），RLS策略按"房间没过期就能读写"，不做更细的按人验证
- 完整schema在`supabase/pk-schema.sql`，已在真实Supabase项目里跑过（用户在SQL Editor执行的）
- **踩过的真bug**：建项目时关掉"automatically expose new tables"，导致`anon`角色对新表**完全没有GRANT权限**，RLS策略本身不会自动带来访问权——报错"permission denied for table pk_rooms"，加了显式`grant select, insert, update on ... to anon`才通过。这个GRANT语句已经写进`pk-schema.sql`了，但**如果以后建新表，一定记得同样要补GRANT，不要只写RLS policy就以为够了**。
- 过期清理**没有配**——`pk-schema.sql`末尾有注释但没有真正配置pg_cron或Edge Function定时跑`delete from pk_rooms where expires_at < now()`。现在Supabase里有几条测试数据（房间码不重要，昵称类似TestFox/FoxB/RaccoonB这种），48小时后按expires_at"逻辑过期"但物理数据不会自动删除，这是已知遗留事项。

**入口**：`grades.html`里独立于年级卡片之外的一张跨年级卡片（渐变`--brand`到`--brand-ink`，文案强调"不分年级看实力"）；`grade7-plus.html`footer里的按钮。**明确决定不做年级门禁**——用户原话"三四年级的可以升级很快到5-6-7年级，不要分年级，要分等级"，所以选难度是PK自己内部的下拉框，不是靠你在哪个年级页面进来决定的。

### 2.5 PK题库内容（17题，pk.js里的`QUESTIONS`数组）

**来源和版权考虑**：不是照抄Math Kangaroo/MOEMS/NRICH的具体题目（这些赛事的题目直接转载有版权问题，即使非商用也不是完全没风险，而且这个仓库是公开的）。做法是：用这些赛事+经典智力题集里反复出现的**公共题型原型**（liar puzzles/单次称重/找规律数列/排除法配对等，几十年来各种题集通用的类型，不专属任何一家）当难度参考，重新编号、换角色、换数字、换语气，这个原则要在以后新增题目时继续遵守。

**第一批13题**（`docs/pk-question-bank-draft.md`详细记录）：DeepSeek通过ds-loop机制起草15题（见2.8），Claude逐题核算：
- 9题原样通过
- 2题发现错误已修正：一道"4×4点阵数正方形"题面和答案(30)对不上（实际4×4点阵应该是14，已把题面改成5×5点阵对齐30）；一道三位数字题答案原来是634，代回条件不对，重新算出653
- 4题剔除：字母找规律(A,E,I,O,U)不算推理题、三人说谎题被发现有两个都成立的答案(逻辑有缺陷)、9枚硬币称重题跟已有题库撞车、下雨地面湿逻辑题跟已上线的m7-02撞车

第二轮4道替换题（原本要补到15题）：2题通过（新序列题、新的必要非充分逻辑题换了雪/温度场景）、2题剔除（一道钟表追及问题DeepSeek算错了方向，正确答案约61分钟不是它说的4分钟；一道立方体涂色题数学没错但跟已通过的题撞车）。**最终第一批止步13题**。

**第二批4题**（pk-14~17，grade3-4难度）：用户明确要求"数字按规律改、描述主体完美替换、用词更幽默"，改编自经典公共原型：
- pk-14 巫师收集青蛙数量找规律（1,4,9,16,__=25，完全平方数）
- pk-15 胖企鹅/瘦企鹅说谎题（简化版三人说谎题，只有2个角色）
- pk-16 3个宝箱找更重的（单次称重，比现有7+那批的"9枚硬币2次称"简单很多）
- pk-17 红蓝绿三只小怪兽零食排除法配对

**踩过的坑（重要，写新题时必须记住）**：pk-15和pk-17起草时也用了跟pk-03同款的"精确文字匹配"判分（`accept:[...]`数组要求完全相等），测试时自己发现"瘦企鹅在撒谎"这种完整句子会被判错——这是**同一类bug第三次出现**（第一次是DeepSeek起草的pk-03、pk-13，第二次是自己手写pk-15、pk-17）。**新写任何"答案是一个词/短语"类型的题，默认应该用`matchAny`（子串包含）而不是`accept`（精确匹配），除非答案本来就该是纯数字这种不太可能被嵌在长句子里的情况。**

### 2.6 判分方式（checkAnswer函数支持的几种question字段）
- `type:'yesno', correct:true/false`：按钮选择题（能/不能），不用文字输入。适用："是否类"逻辑题，避免自由文本判分的脆弱性。
- `accept:[...]`：**精确匹配**（normalize后完全相等），适合纯数字答案（"37"/"653"这种），因为数字类答案不太会被嵌进更长的干扰句子。
- `matchAny:[...]`：**子串包含匹配**，适合"答案是一个词/短语"类题目（"瘦企鹅"/"蓝色"），能正确处理"瘦企鹅在撒谎"这种完整句子。
- `matchLetter:'b'`：单字母提取匹配（正则边界检测，避免"about"里的b被误判），适合"选项字母"类答案（三盒子题的"B"）。
- `acceptAll:[...]`：要求全部token都作为子串出现，适合多值答案（完全平方数个位数字"2,3,7,8"）。

### 2.7 内容分类（category和tier两个独立维度）
- **category**（是否进PK战局）：`puzzle`（逻辑/空间/找规律，不需要列方程，7题g7p+4题g34=11题）vs `academic`（需要设方程求解，6题，全部g7p）。**pickQuestionSet只从category='puzzle'里抽**——用户原话"刚开始出智力题，不要出数学学术题"，学术题6道还留在数组里没删，是给以后"入门/进阶"分档的备用内容，现在完全不进战局。
- **tier**（难度年级）：`g34`（三四年级，4题）vs `g7p`（七年级+，13题，但只有其中7题是puzzle能实际被抽到）。创建房间时下拉框选。
- **已知内容量限制**：g34池子只有4道puzzle题，一局本该抽5题，池子不够就把4道全给，不报错但也凑不满5题——不是bug，是内容量还没跟上，需要以后扩量。

### 2.8 DeepSeek/OpenRouter调用机制（ds-loop）
- **不是MCP工具**，是本机脚本：`~/.claude/scripts/deepseek-call.js`（Windows路径`C:\Users\evolx\.claude\scripts\deepseek-call.js`），直连OpenRouter API调用`deepseek/deepseek-v4-pro`
- 调用方式：`cat prompt.txt | node ~/.claude/scripts/deepseek-call.js`（长prompt用stdin管道，别塞命令行参数，容易有编码/转义问题）
- 依赖`$env:OPENROUTER_API_KEY`环境变量，全新会话能正常读取（不是全新会话时如果刚换过key可能读到旧值，需要手动重新注入，但这个坑跟当前项目无关）
- 全局CLAUDE.md定义了完整的DeepSeek路由规则（数据边界/风险分类/任务类型/规模阈值/调用预算最多3次/失败不静默重试/写入前必须Claude审查/完成后必须报告），**已确认的授权**：符合规则的任务可以直接调用不用每次先问，但**写入前的Claude审查是硬性的，不能省略**——本项目里已经实测DeepSeek起草内容的缺陷率相当高（第一批15题里6题/40%有实质问题，第二批4题里2题/50%有问题），审查环节不是走过场。

### 2.9 彩虹兔子城（用户儿子原创故事，整合进产品的可能性，**目前仅是讨论/概念阶段，没有任何代码实现**)

- **故事文件**：`C:\Users\evolx\Downloads\彩虹兔子城_彩虹钥匙与第六扇门_第一册完整初稿.docx`（不在本仓库里，是Downloads目录下的独立文件）。这台机器**没装pandoc**，读取用`python-docx`（已确认可用）：
  ```python
  import docx
  d = docx.Document(r'路径')
  for p in d.paragraphs:
      if p.text.strip(): print(p.text)
  ```
  注意Windows控制台stdout是cp1252编码，直接print中文会报`UnicodeEncodeError`，要把结果写入UTF-8文件再用Read工具读，不要直接在Bash里print中文。
- **故事设定**：儿童有声读物第一册完整初稿，讲Leo（人类男孩主角）和一群彩色兔子穿越不同"门"的冒险故事。已有角色和能力：
  - Leo（人类主角）——观察力强，善用身边工具解决问题
  - George（校车司机）——不代劳只点拨的导师角色
  - Fred（红兔）——火焰，冲动→学会控制
  - Logan（黄兔）——极速，莽撞→学会等队友
  - Luna（金兔）——激光/找隐藏路径，选择留下没跟队伍走
  - Iris（青蓝兔）——造冰路
  - Sarah（白兔）——控制龙卷风
  - Peter（绿兔）——航海/耐心判断路线
  - Lily（粉兔）——原本独居城堡食物不缺但不快乐，学会主动离开舒适区加入队伍
  - Lulu（紫兔）——被困石室，第一册结尾刚获救
  - Spicy Chicken——一次性对手/喜剧角色，被两个主角合作"解决"（变成炸鸡）
  - 彩虹色几乎用满：红黄金蓝白绿粉紫，**唯独缺橙色**
- **文档自带的创作原则**（必须尊重）：这是孩子的原创作品，"关键创意属于孩子，修改决定也属于孩子"，正文没有擅自加入新反派/重大能力/世界规则或结局。**任何角色建议都只是给家长/孩子讨论用的方向，不应该去改这份docx文件本身**。
- **已给出的评估**（用户问过"故事有趣吗适合几岁"）：主题意识强（多条角色成长线扣同一个"真正的力量是一起找到办法"的主题，这个craft水平在孩子这个年龄段不容易），估计适合8-11岁/三到六年级（跟本产品现有年级分层吻合）。升级建议：偶尔让"能力对应题型"的公式失灵一次增加悬念、给对手加点性格、深挖角色内心（Lily那条线已经证明孩子有这个能力）、偶尔让选择有代价。
- **已讨论但未实现的整合方案**：
  1. 核心原则——**角色应该对应"技能类别"（网站的世界/板块，一个稳定的小数量），不应该对应"单个题目"**（几千上万道题不可能每道配一个专门场景）。这样故事本身不用管题库多大，题库涨多少角色阵容都不用变。
  2. 提议的映射表（多数直接用现有角色，不用都造新的）：

     | 板块 | 角色 | 状态 |
     |---|---|---|
     | 称重推理 | 橙兔子 | 建议新增，补齐彩虹缺色 |
     | 逻辑推演（说谎/谁真谁假） | 巫师 | 建议新增，用户认可，具体是否做成"巫师兔"还是独立于兔子形象，留给孩子决定 |
     | 空间几何 | Peter（现有） | 航海认路本来就空间感强 |
     | 策略博弈 | George（现有） | "办法不是越大越好"这类台词本来就是策略智慧 |
     | AI时代思维（核查/别轻信） | Leo（现有） | 全书主角特质就是"先别急看清楚"，跟"别轻信AI"立意一致 |
     | 组合计数/数论 | Luna或Logan（现有，待定） | 变化的图书馆/找规律气质接近，具体哪个更合适没有定论 |
     | 过河/资源限制策略题（新识别出的类别，之前架构文档没单列） | 鳄鱼 | 用户提议，建议定位成"守关卡出谜题"的角色，跟已有的鲨鱼（单纯危险）区分开不要撞味 |

  3. 真正需要新增的角色：**橙兔（称重）+ 巫师（逻辑）+ 鳄鱼（过河策略）**，一共3个，其余板块安在现有角色身上，不用为了配题库硬造一堆新兔子。
  4. 用户提到"网页到时候增加彩虹兔子城的音频故事"——这个功能**完全没有开始设计或实现**，只是提到了这个未来计划。

---

## 3. 关键的工程/流程习惯（新会话要延续）

- **验证方式**：这个环境里`file://`协议打开仓库外的文件会渲染成静态快照，没有真正的DOM/JS交互能力；仓库内的文件在Browser pane里点开也一样。**真正的交互测试必须起本地server**：复制相关文件到`C:\Users\evolx\.claude\projects\C--Users-evolx-ai-valuation\_preview_tmp\`，在同目录的`.claude\launch.json`临时加一个`python -m http.server`的entry，`preview_start`用`name`（不是`url`）打开，测完**一定要清理**（停server、把launch.json改回原样、删掉`_preview_tmp`）。
- **javascript_tool不支持顶层await**，要用`(async () => { ... })()`包一层。有时候单次调用会报"Browser pane is currently hidden...超时"，这通常是环境瞬时问题不是代码问题——用`get_page_text`/直接查DOM状态确认实际执行到哪一步了，通常代码其实跑完了，只是这次调用汇报超时。
- **两个浏览器tab模拟PK两个玩家**测试的模式：`tabs_create()`开新tab，两边各自走一遍流程，用`javascript_tool`直接操作DOM（填input.value、点击button.click()）比模拟真实键盘输入更可靠。
- **PowerShell处理含中文的文件时，不要用`Get-Content`/`Set-Content`不加`-Encoding`**，会静默产生乱码（这个坑在早期会话吃过一次亏，做Artifact预览时）。拼接/预览类操作改用Node.js的`fs.readFileSync(path,'utf8')`或者直接用Read/Write工具。
- **推送习惯**：这个仓库的改动，验证通过后可以直接`git push origin master`，不用每次都问用户确认（这是用户在早期会话明确给过的标准授权）。
- **DeepSeek起草的内容，写入前必须Claude逐条审查**（不是走流程，是真的要重新推一遍逻辑/算一遍数值）——本项目的实测缺陷率相当高（40-50%），千万不要因为"格式对了"就直接采信。

---

## 4. 当前未解决/待定事项（按优先级）

1. **【最高优先级，来自上一轮的顶级教育专家综合评估】内容验证的自动化**——现在核算全靠Claude人工逐题推理，DeepSeek起草缺陷率40-50%，这个瓶颈不解决，"几千上万道题"的目标做不到。可能方向：写一个自动数值验算脚本（对纯数字答案的题，用代码而不是人工重新算一遍），或者接受"内容增长速度被人工审核卡死"这个现实、调整规模预期。
2. **PK对战content仍然很薄**：g7p可用7题、g34只有4题。用户已经反馈"担心玩几轮就腻"，是设计初衷（解决work感）能否真正兑现的关键——需要持续扩量，且必须延续"公共原型改编，不逐字抄版权题库"的方法论。
3. **过期房间清理任务没配置**：Supabase里已经有测试数据，`expires_at`字段有但没有定时任务真正删除过期行，需要配pg_cron或Edge Function。
4. **"填年级→自动分池"目前只是"选难度池子"，不是真正的自适应**：用户最初设想"再难度自适应"（根据答题表现动态调整），目前内容量（尤其g34只有4题）完全撑不起这个，需要现有池子扩大到十几题以上才有意义去做。
5. **陌生人PK匹配**：明确列为"以后再评估"，涉及儿童隐私/安全合规，需要专门一轮讨论，不能顺手加。
6. **真·实时对战**（双方同时在线看进度条）：明确推迟，异步已验证够用，除非以后有明确需求再评估工程量。
7. **彩虹兔子城整合**：目前只是概念讨论+角色映射建议，**没有任何代码或角色实际写入网站**。如果要往下推进，下一步大概是：(a) 等孩子确认要不要接受"橙兔/巫师/鳄鱼"这三个新角色建议，(b) 把现有网站7个板块（World 1-7）的图标/文案换成对应角色主题，(c) "音频故事"功能完全没设计，需要从头讨论范围（真人朗读？TTS？放在哪个页面？）。
8. **architecture.md的Phase 1**（给现有166题打技能标签）：从很早前就规划了，**至今没有开始**，PK模式的开发抢占了这部分精力。
9. **AI时代思维（World 7）扩容**：之前讨论过在现有3题基础上扩展"建模翻译力/估算量级判断/置信度校准"三个新方向，**只有讨论没有实现**，World 7目前仍然只有3道题。

---

## 5. 如果不知道从哪里开始

按"顶级教育专家综合评估"给出的优先级：**先解决内容验证自动化**（第4节第1条），因为这是后面所有扩量工作（PK扩题、AI时代思维扩容、彩虹兔子城配题）共同的瓶颈，不解决的话每往前走一步都在欠审核债。

如果用户明确说要先做别的（比如先推进彩虹兔子城、先扩PK题库），直接照办即可——上面只是没有更明确指示时的建议顺序，不是必须遵守的规则。
